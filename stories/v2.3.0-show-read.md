# Story: v2.3.0 Show/Read Type Classes

> 实现 Show 和 Read 类型类，提供值到字符串的转换和字符串解析功能。

## 目标

1. **Show 类型类** - 将值转换为可读的字符串表示
2. **Read 类型类** - 将字符串解析回值
3. **预定义实例** - 为常用类型提供默认实现

## 验收标准

### Show 模块 (show.zig)

- [x] Show 类型类定义（showBuf 函数）
- [x] 基本类型实例：i32, i64, u8, u32, u64, usize
- [x] 浮点类型实例：f32, f64
- [x] 布尔类型实例
- [x] 字符串类型实例
- [x] Option 类型实例
- [x] Result 类型实例
- [x] 切片类型实例
- [x] showToWriter 辅助函数
- [x] 单元测试 (14 tests)

### Read 模块 (read.zig)

- [x] Read 类型类定义（read 函数返回 Option）
- [x] 基本整数类型解析：i8, i16, i32, i64, u8, u16, u32, u64, usize
- [x] 浮点类型解析：f32, f64
- [x] 布尔类型解析
- [x] 字符串类型解析
- [x] 十六进制解析：readHexU32, readHexU64
- [x] Option 类型解析
- [x] 辅助函数：parseInt, parseFloat, readMany, readManyStrict
- [x] 单元测试 (13 tests)

### 集成

- [x] algebra/mod.zig 导出
- [x] root.zig 导出
- [x] CHANGELOG.md 更新
- [x] ROADMAP.md 更新
- [x] 测试通过 (991 tests)

## 设计说明

### Show 类型类

```zig
/// Show 类型类 - 将值转换为字符串表示
pub fn Show(comptime T: type) type {
    return struct {
        showBufFn: *const fn (T, []u8) []const u8,
        
        pub fn showBuf(self: @This(), value: T, buf: []u8) []const u8 {
            return self.showBufFn(value, buf);
        }
        
        pub fn showAlloc(self: @This(), allocator: Allocator, value: T) ![]u8 {
            // 使用分配器创建字符串
        }
    };
}
```

### Read 类型类

```zig
/// Read 类型类 - 将字符串解析为值
pub fn Read(comptime T: type) type {
    return struct {
        readFn: *const fn ([]const u8) Option(T),
        
        pub fn read(self: @This(), str: []const u8) Option(T) {
            return self.readFn(str);
        }
        
        pub fn readOr(self: @This(), str: []const u8, default: T) T {
            // 带默认值的解析
        }
    };
}
```

## 完成状态

- 开始日期: 2026-01-02
- 完成日期: 2026-01-02
- 状态: ✅ 已完成
